name: CDS Smoke Test

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'apps/cds/**'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install k6
        run: |
          curl -s https://github.com/grafana/k6/releases/latest/download/k6-latest-linux-amd64.tar.gz -L | tar xz
          sudo mv k6-*/k6 /usr/local/bin/
          k6 version

      - name: Run CDS Smoke Test
        env:
          BASE_URL: ${{ secrets.BASE_URL }}
          TEST_EMAIL: ${{ secrets.TEST_EMAIL }}
          TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}
        run: k6 run -e BASE_URL=$BASE_URL -e TEST_EMAIL=$TEST_EMAIL -e TEST_PASSWORD=$TEST_PASSWORD apps/cds/scripts/smoke.js
